---
import type { ComponentProps } from "react";
import { Tabs } from "@/components/blog/interactive-elements";

type Props = ComponentProps<typeof Tabs>;

const props = Astro.props;
---

<Tabs {...props} client:visible />
